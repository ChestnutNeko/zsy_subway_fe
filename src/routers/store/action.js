/**
 * 路由权限action
 */

const actionType = {
	CONTROLROUTERPERMISSION:'controlRouterPermission', //控制路由权限问题
};

const actions = {
	createControlRouterAction(param) { //派发改变路由权限的action
		return dispatch => {

		}
	}
};

export { actionType, actions }